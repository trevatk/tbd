version: v2
clean: false

managed:
  enabled: false

plugins:
  - local: protoc-gen-go
    out: ../lib/protocol
    opt:
      - paths=source_relative

inputs:
  - directory: proto
    paths:
      - proto/did/v1/document.proto
